{
  "name": "tiandiren-codex",
  "private": true,
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  }
}
